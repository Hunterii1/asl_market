# 📱 مستندات کامل پروژه ASL Market برای ساخت اپلیکیشن Flutter

## 🎯 معرفی پروژه

**ASL Market** یک پلتفرم جامع برای تجارت و بازاریابی محصولات ایرانی است که شامل بخش‌های مختلفی برای تأمین‌کنندگان، ویزیتورها، محصولات تحقیقی، آموزش، هوش مصنوعی و مدیریت مالی می‌باشد.

---

## 🏗️ معماری پروژه

### Backend
- **زبان**: Go (Golang)
- **فریمورک**: Gin
- **دیتابیس**: MySQL با GORM
- **احراز هویت**: JWT Token
- **پورت**: 8080

### Frontend (موجود)
- **زبان**: TypeScript
- **فریمورک**: React + Vite
- **UI Library**: shadcn-ui + Tailwind CSS

### اپلیکیشن Flutter (مورد نیاز)
- باید تمام قابلیت‌های Frontend موجود را داشته باشد
- باید با Backend API ارتباط برقرار کند
- باید از JWT Token برای احراز هویت استفاده کند

---

## 🔐 سیستم احراز هویت و لایسنس

### احراز هویت
- **ثبت‌نام**: با نام، نام خانوادگی، ایمیل (اختیاری)، شماره تلفن و رمز عبور
- **ورود**: با ایمیل یا شماره تلفن + رمز عبور
- **بازیابی رمز**: ارسال کد بازیابی به شماره تلفن
- **Token**: JWT که در localStorage ذخیره می‌شود

### سیستم لایسنس
پروژه از سیستم لایسنس استفاده می‌کند که سه نوع دارد:
- **Plus**: 12 ماه اعتبار
- **Plus4**: 4 ماه اعتبار  
- **Pro**: 30 ماه اعتبار

**قابلیت‌های لایسنس:**
- فعال‌سازی لایسنس با کد
- نمایش اطلاعات لایسنس (نوع، تاریخ انقضا، روزهای باقیمانده)
- بررسی وضعیت لایسنس قبل از دسترسی به بخش‌های خاص
- ارتقای لایسنس (Upgrade Request)
- برخی بخش‌ها نیاز به لایسنس فعال دارند

---

## 📋 بخش‌های اصلی اپلیکیشن

### 1️⃣ داشبورد (Dashboard)
**مسیر API**: `GET /api/v1/dashboard`

**قابلیت‌ها:**
- نمایش آمار برداشت‌های مالی (Withdrawal Stats)
- نمایش درخواست‌های برداشت اخیر
- نمودار تاریخچه برداشت‌ها (30 روز اخیر)
- نمایش پیشرفت کاربر (User Progress)
- نمایش فعالیت‌های انجام شده:
  - آموزش (Tutorial)
  - تأمین‌کنندگان (Suppliers)
  - ویزیتورها (Visitors)
  - هوش مصنوعی (AI)
  - محصولات (Products)
  - برداشت (Withdrawal)
  - کالاهای موجود (Available)
  - ارسال (Express)
  - یادگیری (Learning)

---

### 2️⃣ مدیریت تأمین‌کنندگان (ASL Supplier)

#### ثبت‌نام تأمین‌کننده
**مسیر API**: `POST /api/v1/supplier/register`

**اطلاعات مورد نیاز:**
- اطلاعات شخصی: نام کامل، شماره موبایل، نام برند، تصویر
- اطلاعات مکانی: شهر، آدرس
- اطلاعات کسب‌وکار: 
  - آیا کسب‌وکار ثبت شده دارد؟
  - شماره ثبت کسب‌وکار
  - سند ثبت کسب‌وکار
- تجربه صادرات: آیا تجربه صادرات دارد؟ قیمت‌های صادراتی
- قیمت‌گذاری:
  - حداقل قیمت عمده‌فروشی
  - قیمت برای حجم بالا
  - امکان تولید برچسب اختصاصی
- محصولات: لیست محصولات با جزئیات:
  - نام محصول
  - نوع محصول (غذایی، گیاهی، سلامت، صنایع دستی، صنعتی، خانگی، سایر)
  - توضیحات
  - نیاز به مجوز صادرات
  - نوع مجوز مورد نیاز
  - حداقل تولید ماهانه
  - تصاویر محصول
  - تصاویر بسته‌بندی
  - ویدیوهای فرآیند تولید

#### وضعیت ثبت‌نام
**مسیر API**: `GET /api/v1/supplier/status`

**وضعیت‌ها:**
- `pending`: در انتظار بررسی
- `approved`: تأیید شده
- `rejected`: رد شده

#### مشاهده تأمین‌کنندگان تأیید شده
**مسیر API**: `GET /api/v1/suppliers`

**قابلیت‌ها:**
- لیست تأمین‌کنندگان با صفحه‌بندی
- نمایش تأمین‌کنندگان برگزیده (Featured) در اول لیست
- فیلتر و جستجو
- مشاهده جزئیات هر تأمین‌کننده
- مشاهده اطلاعات تماس (با محدودیت روزانه)

#### محدودیت مشاهده اطلاعات تماس
- هر کاربر در روز می‌تواند تعداد محدودی اطلاعات تماس تأمین‌کنندگان را ببیند
- **مسیر API**: `GET /api/v1/daily-limits/supplier-permission`
- **مسیر API**: `POST /api/v1/contact/view` (برای مشاهده اطلاعات تماس)

---

### 3️⃣ مدیریت ویزیتورها (ASL Visit)

#### ثبت‌نام ویزیتور
**مسیر API**: `POST /api/v1/visitor/register`

**اطلاعات مورد نیاز:**
- اطلاعات هویتی:
  - نام کامل
  - کد ملی
  - شماره پاسپورت
  - تاریخ تولد
  - شماره موبایل
  - شماره واتساپ
  - ایمیل
- اطلاعات محل سکونت:
  - آدرس محل سکونت
  - شهر/استان
  - شهرهای مقصد
  - داشتن تماس محلی
  - جزئیات تماس محلی
- اطلاعات بانکی:
  - شماره شبا
  - نام بانک
  - نام صاحب حساب
- تجربه و مهارت‌ها:
  - تجربه بازاریابی
  - توضیحات تجربه
  - سطح زبان (عالی، خوب، ضعیف، هیچ)
  - مهارت‌های خاص
- محصولات مورد علاقه: لیست محصولاتی که ویزیتور به آن‌ها علاقه دارد
- تعهدات:
  - موافقت با استفاده از محصولات تأیید شده
  - موافقت با عواقب تخلف
  - موافقت با ارسال گزارش
  - امضای دیجیتال
  - تاریخ امضا
- مدارک:
  - تصویر کارت ملی
  - عکس شخصی
  - توصیه‌نامه

#### وضعیت ثبت‌نام
**مسیر API**: `GET /api/v1/visitor/status`

#### مشاهده ویزیتورهای تأیید شده
**مسیر API**: `GET /api/v1/visitors`

**قابلیت‌ها:**
- لیست ویزیتورها با صفحه‌بندی
- نمایش ویزیتورهای برگزیده در اول لیست
- فیلتر و جستجو
- مشاهده جزئیات هر ویزیتور
- مشاهده اطلاعات تماس (با محدودیت روزانه)

#### محدودیت مشاهده اطلاعات تماس
- **مسیر API**: `GET /api/v1/daily-limits/visitor-permission`
- **مسیر API**: `POST /api/v1/contact/view`

---

### 4️⃣ محصولات تحقیقی (Research Products)

**مسیر API**: `GET /api/v1/research-products`

**قابلیت‌ها:**
- مشاهده لیست محصولات تحقیقی
- فیلتر بر اساس دسته‌بندی
- فیلتر بر اساس وضعیت (فعال/غیرفعال)
- جستجو بر اساس HS Code
- مشاهده جزئیات هر محصول:
  - نام محصول
  - دسته‌بندی
  - HS Code
  - توضیحات
  - اطلاعات صادرات
  - کشورهای مقصد
  - تصاویر

**دسته‌بندی‌ها:**
- **مسیر API**: `GET /api/v1/research-products/categories`

---

### 5️⃣ کالاهای موجود (ASL Available)

**مسیر API**: `GET /api/v1/available-products`

**قابلیت‌ها:**
- مشاهده لیست کالاهای موجود
- فیلتر بر اساس دسته‌بندی
- مشاهده کالاهای برگزیده (Featured)
- مشاهده پیشنهادهای ویژه (Hot Deals)
- مشاهده جزئیات هر کالا:
  - نام محصول
  - دسته‌بندی و زیردسته
  - قیمت (عمده‌فروشی، خرده‌فروشی، صادراتی)
  - ارز
  - موجودی
  - حداقل و حداکثر سفارش
  - واحد اندازه‌گیری
  - برند و مدل
  - مبدأ
  - کیفیت
  - نوع بسته‌بندی
  - وزن و ابعاد
  - هزینه ارسال
  - موقعیت مکانی
  - اطلاعات تماس
  - امکان صادرات
  - نیاز به مجوز
  - کشورهای مقصد صادرات
  - تصاویر
  - ویدیو
  - کاتالوگ

#### مدیریت کالاهای کاربر
**مسیر API**: `GET /api/v1/my-products`

**قابلیت‌ها:**
- مشاهده کالاهای اضافه شده توسط کاربر
- افزودن کالای جدید
- ویرایش کالا
- حذف کالا
- آپلود تصاویر چندگانه

#### ثبت کالا
**مسیر API**: `POST /api/v1/submit-product`

---

### 6️⃣ سیستم آموزش (ASL Learn)

**مسیر API**: `GET /api/v1/training/categories`

**قابلیت‌ها:**
- مشاهده دسته‌بندی‌های آموزشی
- مشاهده ویدیوهای هر دسته‌بندی
- جستجوی ویدیوها
- پخش ویدیو با پلیر اختصاصی
- ردیابی ویدیوهای تماشا شده
- آمار تماشای ویدیوها

**API های مربوطه:**
- `GET /api/v1/training/videos` - لیست همه ویدیوها
- `GET /api/v1/training/category/:id/videos` - ویدیوهای یک دسته
- `GET /api/v1/training/video/:id` - جزئیات یک ویدیو
- `GET /api/v1/training/videos/search?q=...` - جستجوی ویدیو
- `POST /api/v1/training/video/:id/watch` - ثبت تماشای ویدیو
- `GET /api/v1/training/watched-videos` - ویدیوهای تماشا شده
- `GET /api/v1/training/watch-stats` - آمار تماشا
- `GET /api/v1/training/video/:id/stream` - استریم ویدیو

---

### 7️⃣ هوش مصنوعی (ASL AI)

**مسیر API**: `POST /api/v1/ai/chat`

**قابلیت‌ها:**
- چت با هوش مصنوعی
- مدیریت چت‌ها (ایجاد، مشاهده، حذف)
- محدودیت روزانه: 20 پیام در روز
- نمایش استفاده روزانه و باقیمانده

**API های مربوطه:**
- `POST /api/v1/ai/chat` - ارسال پیام
- `GET /api/v1/ai/chats` - لیست چت‌ها
- `GET /api/v1/ai/chats/:id` - جزئیات یک چت
- `DELETE /api/v1/ai/chats/:id` - حذف چت
- `GET /api/v1/ai/usage` - آمار استفاده

**محدودیت‌ها:**
- هر کاربر در روز حداکثر 20 پیام می‌تواند ارسال کند
- بعد از رسیدن به حد، باید منتظر بماند تا روز بعد

---

### 8️⃣ سیستم برداشت پول (ASL Pay)

**مسیر API**: `POST /api/v1/withdrawal/request`

**قابلیت‌ها:**
- ثبت درخواست برداشت
- مشاهده درخواست‌های برداشت
- آپلود رسید پرداخت
- مشاهده آمار برداشت‌ها
- نمودار تاریخچه برداشت‌ها

**اطلاعات مورد نیاز برای درخواست برداشت:**
- مبلغ
- ارز (USD, AED, SAR, و غیره)
- کشور مبدأ (AE, SA, KW, و غیره)
- شماره کارت بانکی ایرانی
- نام صاحب کارت
- شماره شبا
- نام بانک

**وضعیت‌های درخواست:**
- `pending`: در انتظار بررسی
- `approved`: تأیید شده
- `processing`: در حال پردازش
- `completed`: تکمیل شده
- `rejected`: رد شده

**API های مربوطه:**
- `POST /api/v1/withdrawal/request` - ثبت درخواست
- `GET /api/v1/withdrawal/requests` - لیست درخواست‌ها
- `GET /api/v1/withdrawal/request/:id` - جزئیات یک درخواست
- `POST /api/v1/withdrawal/receipt/:id` - آپلود رسید
- `GET /api/v1/withdrawal/stats` - آمار برداشت‌ها

---

### 9️⃣ سیستم ارسال (ASL Express)

این بخش در حال توسعه است و برای محاسبه هزینه و زمان ارسال استفاده می‌شود.

---

### 🔟 سیستم نوتیفیکیشن

**مسیر API**: `GET /api/v1/notifications`

**قابلیت‌ها:**
- مشاهده نوتیفیکیشن‌ها
- علامت‌گذاری به عنوان خوانده شده
- علامت‌گذاری همه به عنوان خوانده شده
- نمایش تعداد نوتیفیکیشن‌های خوانده نشده
- فیلتر بر اساس خوانده شده/نخوانده شده

**API های مربوطه:**
- `GET /api/v1/notifications` - لیست نوتیفیکیشن‌ها
- `GET /api/v1/notifications/:id` - جزئیات یک نوتیفیکیشن
- `POST /api/v1/notifications/:id/read` - علامت‌گذاری به عنوان خوانده شده
- `POST /api/v1/notifications/read-all` - علامت‌گذاری همه
- `GET /api/v1/notifications/unread-count` - تعداد خوانده نشده‌ها

---

### 1️⃣1️⃣ سیستم تیکت پشتیبانی

**مسیر API**: `POST /api/v1/support/tickets`

**قابلیت‌ها:**
- ثبت تیکت جدید
- مشاهده تیکت‌های کاربر
- مشاهده جزئیات یک تیکت
- افزودن پیام به تیکت
- بستن تیکت
- فیلتر بر اساس وضعیت

**اطلاعات مورد نیاز برای ثبت تیکت:**
- عنوان
- توضیحات
- اولویت (کم، متوسط، بالا، فوری)
- دسته‌بندی (عمومی، فنی، مالی، لایسنس، سایر)

**وضعیت‌های تیکت:**
- `open`: باز
- `in_progress`: در حال بررسی
- `waiting`: منتظر پاسخ کاربر
- `closed`: بسته شده

**API های مربوطه:**
- `POST /api/v1/support/tickets` - ثبت تیکت
- `GET /api/v1/support/tickets` - لیست تیکت‌ها
- `GET /api/v1/support/tickets/:id` - جزئیات تیکت
- `POST /api/v1/support/tickets/:id/messages` - افزودن پیام
- `POST /api/v1/support/tickets/:id/close` - بستن تیکت

---

### 1️⃣2️⃣ پاپ‌آپ تبلیغاتی (Marketing Popup)

**مسیر API**: `GET /api/v1/marketing-popups/active`

**قابلیت‌ها:**
- نمایش پاپ‌آپ تبلیغاتی فعال
- ردیابی کلیک روی پاپ‌آپ
- نمایش دکمه و لینک تخفیف

**API های مربوطه:**
- `GET /api/v1/marketing-popups/active` - پاپ‌آپ فعال
- `POST /api/v1/marketing-popups/:id/click` - ثبت کلیک

---

## 📤 آپلود فایل

**مسیر API**: `POST /api/v1/upload/...`

**قابلیت‌ها:**
- آپلود تصویر تأمین‌کننده
- آپلود تصویر محصول
- آپلود چند تصویر محصول
- حذف تصویر

**API های مربوطه:**
- `POST /api/v1/upload/supplier-image` - آپلود تصویر تأمین‌کننده
- `POST /api/v1/upload/product-image` - آپلود تصویر محصول
- `POST /api/v1/upload/product-images` - آپلود چند تصویر
- `POST /api/v1/upload/delete-image` - حذف تصویر

---

## 🔄 مدیریت پروفایل

**مسیر API**: `PUT /api/v1/profile`

**قابلیت‌ها:**
- مشاهده اطلاعات کاربر
- ویرایش پروفایل (نام، نام خانوادگی، ایمیل، شماره تلفن)

**API های مربوطه:**
- `GET /api/v1/me` - اطلاعات کاربر فعلی
- `PUT /api/v1/profile` - ویرایش پروفایل

---

## 📊 سیستم پیشرفت کاربر (Progress Tracking)

**مسیر API**: `GET /api/v1/progress`

**قابلیت‌ها:**
- نمایش پیشرفت کلی کاربر
- نمایش فعالیت‌های انجام شده
- نمایش مراحل بعدی
- به‌روزرسانی پیشرفت با انجام فعالیت

**API های مربوطه:**
- `GET /api/v1/progress` - دریافت پیشرفت
- `POST /api/v1/progress/update` - به‌روزرسانی پیشرفت

---

## 🔑 سیستم لایسنس

### فعال‌سازی لایسنس
**مسیر API**: `POST /api/v1/license/verify`

**اطلاعات مورد نیاز:**
- کد لایسنس

**پاسخ:**
- وضعیت فعال‌سازی
- نوع لایسنس
- تاریخ انقضا
- روزهای باقیمانده

### بررسی وضعیت لایسنس
**مسیر API**: `GET /api/v1/license/status`

**پاسخ:**
- آیا کاربر لایسنس دارد؟
- آیا لایسنس فعال است؟
- آیا کاربر تأیید شده است؟

### اطلاعات لایسنس
**مسیر API**: `GET /api/v1/license/info`

**پاسخ:**
- کد لایسنس
- تاریخ فعال‌سازی
- تاریخ انقضا
- نوع لایسنس
- مدت اعتبار (ماه)
- روزهای باقیمانده
- ساعت‌های باقیمانده
- آیا فعال است؟

### به‌روزرسانی لایسنس
**مسیر API**: `POST /api/v1/license/refresh`

---

## 📱 ثبت‌نام عمومی (Public Registration)

### ثبت‌نام تأمین‌کننده عمومی
**مسیر API**: `POST /api/v1/public/supplier/register`

این API برای ثبت‌نام بدون نیاز به احراز هویت است.

### ثبت‌نام ویزیتور عمومی
**مسیر API**: `POST /api/v1/public/visitor/register`

### بررسی وضعیت ثبت‌نام
**مسیر API**: `GET /api/v1/public/registration-status`

---

## 🎨 ویژگی‌های UI/UX

### تم (Theme)
- پشتیبانی از تم تاریک و روشن
- ذخیره تنظیمات تم در localStorage

### صفحه‌بندی (Pagination)
- تمام لیست‌ها از صفحه‌بندی پشتیبانی می‌کنند
- پارامترها: `page` و `per_page`

### فیلتر و جستجو
- فیلتر بر اساس وضعیت
- فیلتر بر اساس دسته‌بندی
- جستجو در لیست‌ها

### نمایش برگزیده‌ها (Featured)
- تأمین‌کنندگان برگزیده
- ویزیتورهای برگزیده
- کالاهای برگزیده
- پیشنهادهای ویژه (Hot Deals)

---

## 🔒 امنیت و محدودیت‌ها

### محدودیت‌های روزانه
- **مشاهده اطلاعات تماس تأمین‌کنندگان**: تعداد محدود در روز
- **مشاهده اطلاعات تماس ویزیتورها**: تعداد محدود در روز
- **استفاده از هوش مصنوعی**: 20 پیام در روز

### احراز هویت
- تمام API های محافظت شده نیاز به JWT Token دارند
- Token در Header به صورت `Authorization: Bearer {token}` ارسال می‌شود

### دسترسی بر اساس لایسنس
- برخی بخش‌ها نیاز به لایسنس فعال دارند:
  - ASL Learn
  - ASL Supplier
  - ASL Express
  - ASL Visit
  - ASL Pay
  - ASL AI
  - ASL Available
  - Products Research

---

## 📡 ساختار API

### Base URL
- **Development**: `http://localhost:8080/api/v1`
- **Production**: `https://asllmarket.com/backend/api/v1`

### فرمت پاسخ
**موفقیت:**
```json
{
  "message": "پیام موفقیت",
  "data": { ... }
}
```

**خطا:**
```json
{
  "error": "پیام خطا",
  "details": "جزئیات خطا (اختیاری)"
}
```

### کدهای وضعیت HTTP
- `200`: موفقیت
- `201`: ایجاد موفق
- `400`: درخواست نامعتبر
- `401`: عدم احراز هویت
- `403`: عدم دسترسی
- `404`: یافت نشد
- `500`: خطای سرور

---

## 🗂️ ساختار دیتابیس

### جداول اصلی
1. **users**: کاربران
2. **suppliers**: تأمین‌کنندگان
3. **visitors**: ویزیتورها
4. **licenses**: لایسنس‌ها
5. **available_products**: کالاهای موجود
6. **research_products**: محصولات تحقیقی
7. **training_categories**: دسته‌بندی‌های آموزشی
8. **training_videos**: ویدیوهای آموزشی
9. **withdrawal_requests**: درخواست‌های برداشت
10. **support_tickets**: تیکت‌های پشتیبانی
11. **notifications**: نوتیفیکیشن‌ها
12. **chats**: چت‌های هوش مصنوعی
13. **ai_usage**: استفاده از هوش مصنوعی
14. **user_progress**: پیشرفت کاربر
15. **contact_view_limits**: محدودیت‌های مشاهده تماس
16. **daily_limits**: محدودیت‌های روزانه
17. **marketing_popups**: پاپ‌آپ‌های تبلیغاتی

---

## 🚀 نکات مهم برای ساخت اپلیکیشن Flutter

### 1. مدیریت State
- استفاده از Provider یا Riverpod برای مدیریت state
- مدیریت وضعیت احراز هویت
- مدیریت وضعیت لایسنس
- Cache کردن داده‌ها

### 2. مدیریت API
- استفاده از `http` یا `dio` برای درخواست‌های HTTP
- مدیریت JWT Token
- مدیریت خطاها و نمایش پیام‌های مناسب
- Retry logic برای درخواست‌های ناموفق

### 3. ذخیره‌سازی محلی
- استفاده از `shared_preferences` برای ذخیره Token
- Cache کردن داده‌ها با `hive` یا `sqflite`

### 4. UI/UX
- طراحی مطابق با UI موجود در وب
- پشتیبانی از RTL (راست به چپ)
- استفاده از فونت‌های فارسی
- نمایش Loading و Error states
- Pull to refresh برای لیست‌ها

### 5. آپلود فایل
- استفاده از `image_picker` برای انتخاب تصویر
- آپلود با `multipart/form-data`
- نمایش پیشرفت آپلود

### 6. پخش ویدیو
- استفاده از `video_player` یا `chewie` برای پخش ویدیو
- پشتیبانی از استریم ویدیو
- ردیابی پیشرفت تماشا

### 7. نوتیفیکیشن
- استفاده از `firebase_messaging` برای Push Notification
- نمایش نوتیفیکیشن‌های داخلی

### 8. امنیت
- ذخیره امن Token
- رمزگذاری داده‌های حساس
- اعتبارسنجی ورودی‌ها

### 9. بهینه‌سازی
- Lazy loading برای لیست‌ها
- Cache کردن تصاویر
- فشرده‌سازی تصاویر قبل از آپلود

### 10. تست
- تست تمام API ها
- تست سناریوهای مختلف
- تست در دستگاه‌های مختلف

---

## 📝 چک‌لیست قابلیت‌ها

### احراز هویت
- [ ] ثبت‌نام
- [ ] ورود
- [ ] بازیابی رمز عبور
- [ ] خروج

### لایسنس
- [ ] فعال‌سازی لایسنس
- [ ] نمایش اطلاعات لایسنس
- [ ] بررسی وضعیت لایسنس
- [ ] درخواست ارتقا

### داشبورد
- [ ] نمایش آمار
- [ ] نمایش پیشرفت
- [ ] نمودار برداشت‌ها

### تأمین‌کنندگان
- [ ] ثبت‌نام
- [ ] مشاهده وضعیت
- [ ] مشاهده لیست
- [ ] مشاهده جزئیات
- [ ] مشاهده اطلاعات تماس

### ویزیتورها
- [ ] ثبت‌نام
- [ ] مشاهده وضعیت
- [ ] مشاهده لیست
- [ ] مشاهده جزئیات
- [ ] مشاهده اطلاعات تماس

### محصولات تحقیقی
- [ ] مشاهده لیست
- [ ] فیلتر و جستجو
- [ ] مشاهده جزئیات

### کالاهای موجود
- [ ] مشاهده لیست
- [ ] فیلتر و جستجو
- [ ] مشاهده جزئیات
- [ ] افزودن کالا
- [ ] ویرایش کالا
- [ ] حذف کالا

### آموزش
- [ ] مشاهده دسته‌بندی‌ها
- [ ] مشاهده ویدیوها
- [ ] پخش ویدیو
- [ ] ردیابی تماشا

### هوش مصنوعی
- [ ] چت با AI
- [ ] مدیریت چت‌ها
- [ ] نمایش آمار استفاده

### برداشت پول
- [ ] ثبت درخواست
- [ ] مشاهده درخواست‌ها
- [ ] آپلود رسید
- [ ] مشاهده آمار

### نوتیفیکیشن
- [ ] مشاهده لیست
- [ ] علامت‌گذاری خوانده شده
- [ ] نمایش تعداد خوانده نشده

### تیکت پشتیبانی
- [ ] ثبت تیکت
- [ ] مشاهده تیکت‌ها
- [ ] افزودن پیام
- [ ] بستن تیکت

### پروفایل
- [ ] مشاهده اطلاعات
- [ ] ویرایش پروفایل

---

## 🎯 نتیجه‌گیری

این مستندات شامل تمام قابلیت‌ها و API های پروژه ASL Market است. برای ساخت اپلیکیشن Flutter باید:

1. تمام API های ذکر شده را پیاده‌سازی کنید
2. UI/UX مشابه وب را طراحی کنید
3. مدیریت State و Cache را پیاده‌سازی کنید
4. امنیت و بهینه‌سازی را در نظر بگیرید
5. تست کامل انجام دهید

**موفق باشید! 🚀**

