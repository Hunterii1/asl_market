# 📋 RFP - سیستم Matching تأمین‌کنندگان و ویزیتورها (ASL Matching)

## 🎯 خلاصه اجرایی

### عنوان پروژه
**سیستم Matching تأمین‌کنندگان و ویزیتورها با الگوی اسنپ (Level Up)**

### هدف
ایجاد یک سیستم هوشمند برای اتصال تأمین‌کنندگان ایرانی به ویزیتورهای فعال در کشورهای عربی، با الهام از سیستم matching پلتفرم اسنپ. این سیستم به تأمین‌کنندگان امکان می‌دهد درخواست فروش کالاهای خود را ثبت کنند و ویزیتورهای مناسب به صورت خودکار یا دستی با آن‌ها match شوند.

---

## 📖 شرح کامل فیچر

### 1. مشکل و نیاز

**مشکل فعلی:**
- تأمین‌کنندگان ایرانی که می‌خواهند محصولات خود را به کشورهای عربی صادر کنند، باید به صورت دستی با ویزیتورها ارتباط برقرار کنند
- ویزیتورها باید به صورت دستی در لیست تأمین‌کنندگان جستجو کنند
- فرآیند پیدا کردن همکار مناسب زمان‌بر و ناکارآمد است
- عدم وجود سیستم matching هوشمند برای اتصال خودکار طرفین

**نیاز:**
- سیستم matching خودکار/نیمه‌خودکار بین تأمین‌کنندگان و ویزیتورها
- امکان ثبت درخواست فروش توسط تأمین‌کننده
- اطلاع‌رسانی خودکار به ویزیتورهای مناسب
- مدیریت زمان انقضای درخواست‌ها
- سیستم امتیازدهی و رتبه‌بندی (مثل اسنپ)

---

### 2. جریان کار (Workflow)

#### 2.1 ثبت درخواست توسط تأمین‌کننده

**مرحله 1: تأمین‌کننده درخواست ایجاد می‌کند**

تأمین‌کننده وارد بخش "ASL Matching" می‌شود و درخواست جدید ثبت می‌کند:

**اطلاعات مورد نیاز:**
- **محصول**: انتخاب از لیست محصولات ثبت شده تأمین‌کننده یا ثبت محصول جدید
- **مقدار موجودی**: مقدار کالای موجود (مثلاً: 1000 کیلوگرم، 500 بسته)
- **واحد اندازه‌گیری**: کیلوگرم، تن، بسته، کارتن، و غیره
- **کشورهای مقصد**: انتخاب یک یا چند کشور عربی (امارات، عربستان، کویت، قطر، بحرین، عمان)
- **قیمت پیشنهادی**: قیمت واحد یا قیمت برای حجم مشخص
- **ارز**: USD، AED، SAR، و غیره
- **شرایط پرداخت**: پیش‌پرداخت، پرداخت در تحویل، اعتبار، و غیره
- **زمان تحویل**: مدت زمان آماده‌سازی و ارسال
- **توضیحات اضافی**: هر توضیحی که تأمین‌کننده می‌خواهد اضافه کند
- **زمان انقضا**: تاریخ و ساعت انقضای درخواست (مثلاً: 7 روز، 14 روز، 30 روز)

**مرحله 2: سیستم درخواست را پردازش می‌کند**

- سیستم درخواست را ذخیره می‌کند
- وضعیت درخواست: `pending` (در انتظار matching)
- سیستم شروع به پیدا کردن ویزیتورهای مناسب می‌کند

#### 2.2 Matching هوشمند

**الگوریتم Matching:**

سیستم بر اساس معیارهای زیر ویزیتورهای مناسب را پیدا می‌کند:

1. **کشور مقصد**: ویزیتور باید در کشورهای انتخاب شده توسط تأمین‌کننده فعال باشد
2. **محصولات مورد علاقه**: ویزیتور باید به محصول درخواست شده علاقه داشته باشد (از فیلد `InterestedProducts`)
3. **وضعیت تأیید**: ویزیتور باید تأیید شده باشد (`status = 'approved'`)
4. **سطح زبان**: ویزیتور باید سطح زبان مناسبی داشته باشد (برای ارتباط با خریداران محلی)
5. **تجربه بازاریابی**: اولویت با ویزیتورهایی که تجربه بازاریابی دارند
6. **امتیاز (Rating)**: ویزیتورهایی با امتیاز بالاتر اولویت دارند
7. **فعالیت اخیر**: ویزیتورهایی که اخیراً فعال بوده‌اند اولویت دارند

**نتیجه Matching:**
- لیست ویزیتورهای مناسب به تأمین‌کننده نمایش داده می‌شود
- تأمین‌کننده می‌تواند ویزیتورها را ببیند و انتخاب کند
- یا سیستم به صورت خودکار به همه ویزیتورهای مناسب اطلاع می‌دهد

#### 2.3 اطلاع‌رسانی به ویزیتورها

**روش اطلاع‌رسانی:**

1. **SMS به کاربران اصلی مارکت:**
   - به تمام کاربرانی که لایسنس فعال دارند و ویزیتور تأیید شده هستند
   - محتوای SMS:
     ```
     سلام [نام کاربر]
     
     یک درخواست فروش جدید در ASL Market ثبت شده است:
     
     محصول: [نام محصول]
     مقدار: [مقدار] [واحد]
     کشورهای مقصد: [لیست کشورها]
     قیمت: [قیمت] [ارز]
     
     آیا می‌پذیرید این کار را انجام دهید؟
     
     برای مشاهده جزئیات و پذیرش درخواست، وارد اپلیکیشن شوید.
     
     زمان باقیمانده: [X] روز
     
     ASL Market
     ```

2. **Push Notification در اپلیکیشن:**
   - نوتیفیکیشن به ویزیتورهای مناسب ارسال می‌شود
   - با کلیک روی نوتیفیکیشن، ویزیتور به صفحه جزئیات درخواست هدایت می‌شود

3. **نوتیفیکیشن درون‌برنامه‌ای:**
   - در بخش "درخواست‌های Matching" در اپلیکیشن نمایش داده می‌شود

#### 2.4 پاسخ ویزیتور

**گزینه‌های ویزیتور:**

1. **پذیرش درخواست:**
   - ویزیتور درخواست را می‌پذیرد
   - وضعیت درخواست: `accepted` (پذیرفته شده)
   - تأمین‌کننده مطلع می‌شود
   - ارتباط بین تأمین‌کننده و ویزیتور برقرار می‌شود

2. **رد درخواست:**
   - ویزیتور درخواست را رد می‌کند
   - وضعیت درخواست: `rejected` (رد شده)
   - درخواست به ویزیتورهای دیگر ارسال می‌شود (اگر هنوز زمان باقی مانده باشد)

3. **درخواست اطلاعات بیشتر:**
   - ویزیتور می‌تواند سوال بپرسد
   - تأمین‌کننده پاسخ می‌دهد
   - سپس ویزیتور تصمیم می‌گیرد

#### 2.5 مدیریت زمان انقضا

**سیستم انقضا:**

- هر درخواست یک زمان انقضا دارد (توسط تأمین‌کننده تعیین می‌شود)
- سیستم به صورت خودکار زمان باقیمانده را محاسبه و نمایش می‌دهد
- وقتی زمان به پایان می‌رسد:
  - اگر درخواست پذیرفته نشده باشد: وضعیت به `expired` تغییر می‌کند
  - اگر درخواست پذیرفته شده باشد: درخواست فعال می‌ماند
- تأمین‌کننده می‌تواند زمان انقضا را تمدید کند
- تأمین‌کننده می‌تواند درخواست را قبل از انقضا لغو کند

#### 2.6 پیگیری و مدیریت

**بعد از پذیرش:**

- تأمین‌کننده و ویزیتور می‌توانند با هم چت کنند
- ویزیتور می‌تواند گزارش پیشرفت ارسال کند
- تأمین‌کننده می‌تواند وضعیت فروش را مشاهده کند
- سیستم امتیازدهی: بعد از تکمیل معامله، هر طرف به طرف دیگر امتیاز می‌دهد

---

## 🏗️ نیازمندی‌های فنی

### 3.1 ساختار دیتابیس

#### جدول `matching_requests`

```sql
CREATE TABLE matching_requests (
    id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    supplier_id INT UNSIGNED NOT NULL,
    supplier_product_id INT UNSIGNED,
    product_name VARCHAR(255) NOT NULL,
    product_type VARCHAR(50),
    quantity DECIMAL(10, 2) NOT NULL,
    unit VARCHAR(50) NOT NULL,
    destination_countries TEXT NOT NULL, -- JSON array
    price DECIMAL(10, 2),
    currency VARCHAR(10) DEFAULT 'USD',
    payment_terms TEXT,
    delivery_time VARCHAR(100),
    description TEXT,
    status VARCHAR(20) DEFAULT 'pending', -- pending, accepted, rejected, expired, cancelled, completed
    expires_at DATETIME NOT NULL,
    accepted_by INT UNSIGNED, -- visitor_id
    accepted_at DATETIME,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted_at TIMESTAMP NULL,
    
    FOREIGN KEY (supplier_id) REFERENCES suppliers(id),
    FOREIGN KEY (supplier_product_id) REFERENCES supplier_products(id),
    FOREIGN KEY (accepted_by) REFERENCES visitors(id),
    INDEX idx_status (status),
    INDEX idx_expires_at (expires_at),
    INDEX idx_supplier_id (supplier_id)
);
```

#### جدول `matching_responses`

```sql
CREATE TABLE matching_responses (
    id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    matching_request_id INT UNSIGNED NOT NULL,
    visitor_id INT UNSIGNED NOT NULL,
    response_type VARCHAR(20) NOT NULL, -- accepted, rejected, question
    message TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (matching_request_id) REFERENCES matching_requests(id),
    FOREIGN KEY (visitor_id) REFERENCES visitors(id),
    INDEX idx_request_id (matching_request_id),
    INDEX idx_visitor_id (visitor_id)
);
```

#### جدول `matching_ratings`

```sql
CREATE TABLE matching_ratings (
    id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    matching_request_id INT UNSIGNED NOT NULL,
    supplier_id INT UNSIGNED NOT NULL,
    visitor_id INT UNSIGNED NOT NULL,
    rating_by VARCHAR(20) NOT NULL, -- supplier, visitor
    rating INT NOT NULL, -- 1 to 5
    comment TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (matching_request_id) REFERENCES matching_requests(id),
    FOREIGN KEY (supplier_id) REFERENCES suppliers(id),
    FOREIGN KEY (visitor_id) REFERENCES visitors(id),
    UNIQUE KEY unique_rating (matching_request_id, rating_by)
);
```

#### جدول `matching_notifications`

```sql
CREATE TABLE matching_notifications (
    id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    matching_request_id INT UNSIGNED NOT NULL,
    user_id INT UNSIGNED NOT NULL,
    notification_type VARCHAR(50) NOT NULL, -- sms_sent, push_sent, in_app
    sent_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (matching_request_id) REFERENCES matching_requests(id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    INDEX idx_request_id (matching_request_id),
    INDEX idx_user_id (user_id)
);
```

### 3.2 API Endpoints

#### 3.2.1 مدیریت درخواست‌ها (تأمین‌کننده)

```
POST   /api/v1/matching/requests
       - ایجاد درخواست جدید

GET    /api/v1/matching/requests
       - لیست درخواست‌های تأمین‌کننده

GET    /api/v1/matching/requests/:id
       - جزئیات یک درخواست

PUT    /api/v1/matching/requests/:id
       - ویرایش درخواست

DELETE /api/v1/matching/requests/:id
       - لغو درخواست

POST   /api/v1/matching/requests/:id/extend
       - تمدید زمان انقضا
```

#### 3.2.2 مشاهده و پاسخ به درخواست‌ها (ویزیتور)

```
GET    /api/v1/matching/available-requests
       - لیست درخواست‌های موجود برای ویزیتور

GET    /api/v1/matching/requests/:id
       - جزئیات یک درخواست

POST   /api/v1/matching/requests/:id/accept
       - پذیرش درخواست

POST   /api/v1/matching/requests/:id/reject
       - رد درخواست

POST   /api/v1/matching/requests/:id/question
       - ارسال سوال
```

#### 3.2.3 Matching و جستجو

```
GET    /api/v1/matching/suggested-visitors/:request_id
       - لیست ویزیتورهای پیشنهادی برای یک درخواست

POST   /api/v1/matching/auto-match/:request_id
       - Matching خودکار درخواست با ویزیتورها
```

#### 3.2.4 مدیریت ارتباطات

```
GET    /api/v1/matching/requests/:id/messages
       - لیست پیام‌های بین تأمین‌کننده و ویزیتور

POST   /api/v1/matching/requests/:id/messages
       - ارسال پیام
```

#### 3.2.5 امتیازدهی

```
POST   /api/v1/matching/requests/:id/rate
       - ثبت امتیاز
```

### 3.3 مدل‌های Go

```go
// MatchingRequest model
type MatchingRequest struct {
    ID                uint      `json:"id" gorm:"primaryKey"`
    SupplierID        uint      `json:"supplier_id" gorm:"not null;index"`
    Supplier          Supplier  `json:"supplier" gorm:"foreignKey:SupplierID"`
    SupplierProductID *uint     `json:"supplier_product_id"`
    SupplierProduct   *SupplierProduct `json:"supplier_product,omitempty" gorm:"foreignKey:SupplierProductID"`
    
    ProductName        string    `json:"product_name" gorm:"size:255;not null"`
    ProductType        string    `json:"product_type" gorm:"size:50"`
    Quantity           float64   `json:"quantity" gorm:"type:decimal(10,2);not null"`
    Unit               string    `json:"unit" gorm:"size:50;not null"`
    DestinationCountries string   `json:"destination_countries" gorm:"type:text;not null"` // JSON array
    Price              float64   `json:"price" gorm:"type:decimal(10,2)"`
    Currency           string    `json:"currency" gorm:"size:10;default:'USD'"`
    PaymentTerms       string    `json:"payment_terms" gorm:"type:text"`
    DeliveryTime       string    `json:"delivery_time" gorm:"size:100"`
    Description        string    `json:"description" gorm:"type:text"`
    
    Status             string    `json:"status" gorm:"size:20;default:'pending'"`
    ExpiresAt          time.Time `json:"expires_at" gorm:"not null"`
    AcceptedBy         *uint     `json:"accepted_by"`
    Visitor            *Visitor  `json:"visitor,omitempty" gorm:"foreignKey:AcceptedBy"`
    AcceptedAt         *time.Time `json:"accepted_at"`
    
    CreatedAt          time.Time      `json:"created_at"`
    UpdatedAt          time.Time      `json:"updated_at"`
    DeletedAt          gorm.DeletedAt `json:"-" gorm:"index"`
}
```

### 3.4 سرویس SMS

**یکپارچه‌سازی با سرویس SMS موجود:**
- استفاده از `SMS Service` موجود در پروژه
- ارسال SMS به ویزیتورهای مناسب
- ثبت لاگ ارسال SMS در جدول `matching_notifications`

### 3.5 Job Scheduler برای انقضا

**نیازمندی:**
- یک Job Scheduler (مثل cron job) که هر ساعت یا هر 5 دقیقه اجرا شود
- بررسی درخواست‌هایی که `expires_at` آن‌ها گذشته است
- تغییر وضعیت به `expired`
- اطلاع‌رسانی به تأمین‌کننده

---

## 🎨 نیازمندی‌های UI/UX

### 4.1 صفحه ایجاد درخواست (تأمین‌کننده)

**عناصر UI:**
- فرم ایجاد درخواست با فیلدهای زیر:
  - انتخاب محصول (dropdown یا جستجو)
  - مقدار (input number)
  - واحد (dropdown)
  - انتخاب کشورهای مقصد (multi-select)
  - قیمت (input number)
  - ارز (dropdown)
  - شرایط پرداخت (textarea)
  - زمان تحویل (input text)
  - توضیحات (textarea)
  - زمان انقضا (date picker + time picker)
- دکمه "ثبت درخواست"
- نمایش پیش‌نمایش درخواست قبل از ثبت

### 4.2 صفحه لیست درخواست‌ها (تأمین‌کننده)

**عناصر UI:**
- کارت‌های درخواست با اطلاعات:
  - نام محصول
  - مقدار و واحد
  - کشورهای مقصد
  - قیمت
  - وضعیت (با رنگ‌بندی)
  - زمان باقیمانده (countdown timer)
  - تعداد ویزیتورهای پیشنهادی
  - تعداد پاسخ‌ها
- فیلتر بر اساس وضعیت
- جستجو
- مرتب‌سازی

### 4.3 صفحه درخواست‌های موجود (ویزیتور)

**عناصر UI:**
- کارت‌های درخواست با اطلاعات:
  - نام محصول
  - مقدار و واحد
  - کشورهای مقصد
  - قیمت
  - نام تأمین‌کننده
  - امتیاز تأمین‌کننده
  - زمان باقیمانده
  - دکمه "مشاهده جزئیات"
  - دکمه "پذیرش"
  - دکمه "رد"
- فیلتر بر اساس کشور
- فیلتر بر اساس نوع محصول
- جستجو

### 4.4 صفحه جزئیات درخواست

**برای تأمین‌کننده:**
- تمام اطلاعات درخواست
- لیست ویزیتورهای پیشنهادی
- لیست پاسخ‌های ویزیتورها
- دکمه "تمدید زمان"
- دکمه "لغو درخواست"

**برای ویزیتور:**
- تمام اطلاعات درخواست
- اطلاعات تأمین‌کننده
- دکمه "پذیرش"
- دکمه "رد"
- دکمه "ارسال سوال"

### 4.5 صفحه چت بین تأمین‌کننده و ویزیتور

**عناصر UI:**
- لیست پیام‌ها
- فیلد ارسال پیام
- دکمه ارسال
- نمایش زمان ارسال
- نمایش وضعیت خوانده شده

---

## 📱 نیازمندی‌های اپلیکیشن Flutter

### 5.1 صفحات مورد نیاز

1. **صفحه Matching اصلی**
   - برای تأمین‌کننده: دکمه "ایجاد درخواست جدید"
   - برای ویزیتور: لیست درخواست‌های موجود

2. **صفحه ایجاد درخواست**
   - فرم کامل با تمام فیلدها

3. **صفحه لیست درخواست‌ها**
   - برای تأمین‌کننده: درخواست‌های خود
   - برای ویزیتور: درخواست‌های موجود

4. **صفحه جزئیات درخواست**
   - نمایش کامل اطلاعات
   - دکمه‌های عملیاتی

5. **صفحه چت**
   - چت بین تأمین‌کننده و ویزیتور

### 5.2 قابلیت‌های مورد نیاز

- **Push Notification**: دریافت نوتیفیکیشن برای درخواست‌های جدید
- **Countdown Timer**: نمایش زمان باقیمانده
- **Real-time Updates**: به‌روزرسانی لحظه‌ای وضعیت درخواست‌ها
- **Image Upload**: آپلود تصاویر محصول (اگر نیاز باشد)
- **Map Integration**: نمایش موقعیت تأمین‌کننده و ویزیتور (اختیاری)

---

## 🔔 سیستم اطلاع‌رسانی

### 6.1 SMS

**زمان ارسال:**
- بلافاصله بعد از ثبت درخواست
- فقط به ویزیتورهای مناسب (بر اساس الگوریتم matching)

**محتوای SMS:**
- متن فارسی
- شامل: نام محصول، مقدار، کشورها، قیمت، زمان باقیمانده
- لینک به اپلیکیشن (اختیاری)

### 6.2 Push Notification

**زمان ارسال:**
- بلافاصله بعد از ثبت درخواست
- وقتی ویزیتور درخواست را می‌پذیرد (به تأمین‌کننده)
- وقتی درخواست رد می‌شود (به تأمین‌کننده)
- وقتی زمان انقضا نزدیک می‌شود (24 ساعت قبل)

### 6.3 نوتیفیکیشن درون‌برنامه‌ای

- نمایش در بخش "نوتیفیکیشن‌ها"
- Badge برای تعداد خوانده نشده

---

## 📊 آمار و گزارش‌گیری

### 7.1 آمار برای تأمین‌کننده

- تعداد کل درخواست‌ها
- تعداد درخواست‌های پذیرفته شده
- تعداد درخواست‌های رد شده
- تعداد درخواست‌های منقضی شده
- نرخ موفقیت (درصد پذیرش)
- میانگین زمان پذیرش

### 7.2 آمار برای ویزیتور

- تعداد درخواست‌های دریافت شده
- تعداد درخواست‌های پذیرفته شده
- تعداد درخواست‌های رد شده
- امتیاز متوسط دریافت شده

### 7.3 آمار کلی (Admin)

- تعداد کل درخواست‌ها
- تعداد matching موفق
- نرخ موفقیت کلی
- محبوب‌ترین کشورهای مقصد
- محبوب‌ترین محصولات

---

## 🔒 امنیت و محدودیت‌ها

### 8.1 محدودیت‌ها

- تأمین‌کننده باید تأیید شده باشد (`status = 'approved'`)
- ویزیتور باید تأیید شده باشد (`status = 'approved'`)
- هر تأمین‌کننده می‌تواند حداکثر X درخواست فعال همزمان داشته باشد (مثلاً 5)
- هر ویزیتور می‌تواند حداکثر X درخواست را همزمان بپذیرد (مثلاً 3)

### 8.2 امنیت

- احراز هویت برای تمام API ها
- بررسی دسترسی: تأمین‌کننده فقط می‌تواند درخواست‌های خود را ببیند
- بررسی دسترسی: ویزیتور فقط می‌تواند درخواست‌های مناسب خود را ببیند
- رمزگذاری اطلاعات حساس

---

## ✅ چک‌لیست پیاده‌سازی

### Backend
- [ ] ایجاد جداول دیتابیس
- [ ] ایجاد مدل‌های Go
- [ ] پیاده‌سازی API endpoints
- [ ] پیاده‌سازی الگوریتم matching
- [ ] یکپارچه‌سازی با SMS Service
- [ ] پیاده‌سازی Job Scheduler برای انقضا
- [ ] تست API ها

### Frontend (Web)
- [ ] صفحه ایجاد درخواست
- [ ] صفحه لیست درخواست‌ها
- [ ] صفحه جزئیات درخواست
- [ ] صفحه چت
- [ ] یکپارچه‌سازی با API ها

### Flutter App
- [ ] صفحه Matching اصلی
- [ ] صفحه ایجاد درخواست
- [ ] صفحه لیست درخواست‌ها
- [ ] صفحه جزئیات درخواست
- [ ] صفحه چت
- [ ] Push Notification
- [ ] Countdown Timer
- [ ] یکپارچه‌سازی با API ها

### تست
- [ ] تست سناریوهای مختلف
- [ ] تست matching algorithm
- [ ] تست SMS sending
- [ ] تست expiration
- [ ] تست UI/UX

---

## 📅 جدول زمانی پیشنهادی

### فاز 1: Backend (2 هفته)
- طراحی دیتابیس
- پیاده‌سازی API ها
- الگوریتم matching
- تست Backend

### فاز 2: Frontend Web (1 هفته)
- طراحی UI
- پیاده‌سازی صفحات
- یکپارچه‌سازی با API

### فاز 3: Flutter App (2 هفته)
- طراحی UI
- پیاده‌سازی صفحات
- Push Notification
- یکپارچه‌سازی با API

### فاز 4: تست و بهینه‌سازی (1 هفته)
- تست کامل
- رفع باگ‌ها
- بهینه‌سازی

**کل زمان: 6 هفته**

---

## 💰 برآورد هزینه (اختیاری)

- توسعه Backend: X ساعت
- توسعه Frontend Web: X ساعت
- توسعه Flutter App: X ساعت
- تست: X ساعت
- هزینه SMS: بر اساس تعداد ارسال

---

---

## 🔧 نیازمندی‌های اضافی و بهبودها

### 9.1 بررسی و رفع مشکل ساخت کاربر در اصل مارکت

**مشکل:**
- گاهی اوقات در فرآیند ثبت‌نام کاربران در اصل مارکت مشکلاتی رخ می‌دهد
- ممکن است کاربران duplicate ایجاد شوند
- ممکن است اطلاعات کاربر به درستی ذخیره نشود

**اقدامات مورد نیاز:**

1. **بررسی کدهای موجود:**
   - بررسی `auth_controller.go` - تابع `Register`
   - بررسی `public_registration_controller.go` - توابع `RegisterPublicSupplier` و `RegisterPublicVisitor`
   - بررسی وجود duplicate check برای شماره تلفن و ایمیل
   - بررسی transaction handling

2. **رفع مشکلات احتمالی:**
   - اطمینان از وجود unique constraint روی `phone` و `email`
   - استفاده از transaction برای عملیات چند مرحله‌ای
   - بهبود error handling
   - اضافه کردن logging برای debugging

3. **تست:**
   - تست ثبت‌نام با شماره تلفن تکراری
   - تست ثبت‌نام با ایمیل تکراری
   - تست ثبت‌نام همزمان (race condition)
   - تست rollback در صورت خطا

**API های مورد بررسی:**
```
POST /api/v1/auth/register
POST /api/v1/public/supplier/register
POST /api/v1/public/visitor/register
```

---

### 9.2 سیستم توزیع درخواست‌ها (مثل اجاره)

**نیازمندی:**
سیستم باید درخواست‌ها را به صورت عادلانه بین ویزیتورها توزیع کند.

**قوانین توزیع:**
- اگر 50 درخواست وجود دارد و 5 ویزیتور مناسب
- هر ویزیتور باید 10 درخواست دریافت کند (50 ÷ 5 = 10)
- توزیع باید به صورت round-robin باشد
- اگر تعداد درخواست‌ها بر تعداد ویزیتورها تقسیم نشد، باقیمانده به صورت عادلانه توزیع شود

**الگوریتم توزیع:**

```go
// مثال الگوریتم توزیع
func DistributeRequests(requests []MatchingRequest, visitors []Visitor) map[uint][]uint {
    distribution := make(map[uint][]uint)
    
    // محاسبه تعداد درخواست برای هر ویزیتور
    requestsPerVisitor := len(requests) / len(visitors)
    remainder := len(requests) % len(visitors)
    
    requestIndex := 0
    
    for i, visitor := range visitors {
        count := requestsPerVisitor
        if i < remainder {
            count++ // ویزیتورهای اول باقیمانده را دریافت می‌کنند
        }
        
        for j := 0; j < count && requestIndex < len(requests); j++ {
            distribution[visitor.ID] = append(distribution[visitor.ID], requests[requestIndex].ID)
            requestIndex++
        }
    }
    
    return distribution
}
```

**پیاده‌سازی:**
- در API `POST /api/v1/matching/auto-match/:request_id`
- توزیع خودکار درخواست‌ها بین ویزیتورهای مناسب
- ثبت توزیع در جدول `matching_notifications`
- اطمینان از اینکه هر ویزیتور تعداد مساوی درخواست دریافت می‌کند

**مثال:**
- 50 درخواست موجود
- 5 ویزیتور مناسب
- نتیجه: هر ویزیتور 10 درخواست دریافت می‌کند

---

### 9.3 بررسی و پیاده‌سازی Push Notification در وب اپ

**وضعیت فعلی:**
- PWA با `vite-plugin-pwa` پیاده‌سازی شده است
- Service Worker (`sw.js`) وجود دارد
- Manifest.json تنظیم شده است

**نیازمندی‌ها:**

1. **بررسی قابلیت Push Notification:**
   - بررسی پشتیبانی مرورگر از Web Push API
   - بررسی تنظیمات Service Worker
   - بررسی تنظیمات Notification API

2. **پیاده‌سازی Push Notification:**
   - یکپارچه‌سازی با Firebase Cloud Messaging (FCM) یا Web Push API
   - ثبت subscription کاربران
   - ارسال Push Notification از Backend
   - نمایش Notification در مرورگر

3. **API های مورد نیاز:**
```
POST /api/v1/notifications/subscribe
   - ثبت subscription برای Push Notification
   
POST /api/v1/notifications/unsubscribe
   - لغو subscription
   
GET  /api/v1/notifications/subscription-status
   - بررسی وضعیت subscription
```

4. **Frontend:**
   - درخواست مجوز Notification از کاربر
   - ثبت Service Worker
   - مدیریت Push events
   - نمایش Notification

**کد نمونه (Frontend):**
```typescript
// درخواست مجوز
async function requestNotificationPermission() {
  if ('Notification' in window) {
    const permission = await Notification.requestPermission();
    if (permission === 'granted') {
      // ثبت subscription
      const registration = await navigator.serviceWorker.ready;
      const subscription = await registration.pushManager.subscribe({
        userVisibleOnly: true,
        applicationServerKey: VAPID_PUBLIC_KEY
      });
      
      // ارسال subscription به Backend
      await apiService.subscribeToNotifications(subscription);
    }
  }
}
```

**Backend:**
- ذخیره subscription در دیتابیس
- ارسال Push Notification هنگام ایجاد درخواست matching
- استفاده از Web Push library (مثل `web-push` برای Node.js یا معادل Go)

---

### 9.4 لوگو تم لایت در همه جا

**مشکل:**
- لوگو باید در تم لایت (light theme) به درستی نمایش داده شود
- ممکن است در برخی صفحات لوگو با پس‌زمینه تیره مشکل داشته باشد

**اقدامات:**

1. **بررسی لوگو فعلی:**
   - لوگو در `/LOGO.png` ذخیره شده است
   - Component `Logo.tsx` وجود دارد
   - باید بررسی شود که در تم لایت به درستی نمایش داده می‌شود

2. **راه‌حل‌ها:**
   - ایجاد نسخه لایت و دارک از لوگو
   - استفاده از CSS filter برای تغییر رنگ لوگو بر اساس تم
   - استفاده از SVG برای لوگو که قابل تغییر رنگ است

3. **پیاده‌سازی:**

**گزینه 1: دو نسخه لوگو**
```typescript
// Logo.tsx
export const Logo: React.FC<LogoProps> = ({ className = "", size = 24 }) => {
  const { theme } = useTheme();
  const logoSrc = theme === 'dark' ? '/LOGO-dark.png' : '/LOGO-light.png';
  
  return (
    <img 
      src={logoSrc}
      alt="اصل مارکت" 
      className={`object-contain ${className}`}
      style={{ width: size, height: size }}
    />
  );
};
```

**گزینه 2: استفاده از CSS Filter**
```css
.logo-light {
  filter: brightness(0) invert(1); /* سفید */
}

.logo-dark {
  filter: brightness(0); /* سیاه */
}
```

**گزینه 3: SVG با تغییر رنگ**
```tsx
export const Logo: React.FC<LogoProps> = ({ className = "", size = 24 }) => {
  const { theme } = useTheme();
  const logoColor = theme === 'dark' ? '#ffffff' : '#000000';
  
  return (
    <svg width={size} height={size} viewBox="0 0 24 24" className={className}>
      {/* SVG paths با رنگ logoColor */}
    </svg>
  );
};
```

4. **بررسی صفحات:**
   - بررسی تمام صفحات که از Logo استفاده می‌کنند
   - اطمینان از نمایش صحیح در تم لایت
   - تست در مرورگرهای مختلف

**صفحات مورد بررسی:**
- Header (تمام صفحات)
- صفحه اصلی (Index)
- صفحات ثبت‌نام
- صفحات لاگین
- Footer (اگر وجود دارد)

---

### 9.5 بهبودهای UI/UX اضافی

**نیازمندی‌های عمومی:**
- اطمینان از سازگاری با تم لایت و دارک
- بهبود responsive design
- بهبود accessibility
- بهینه‌سازی performance

---

## 🎯 نتیجه‌گیری

این سیستم matching به تأمین‌کنندگان و ویزیتورها کمک می‌کند تا به راحتی با هم ارتباط برقرار کنند و معاملات خود را انجام دهند. با الهام از سیستم matching اسنپ، این فیچر تجربه کاربری بهتری را فراهم می‌کند و فرآیند صادرات را تسهیل می‌کند.

**بهبودهای اضافی:**
- رفع مشکلات ثبت‌نام کاربران
- توزیع عادلانه درخواست‌ها بین ویزیتورها
- پیاده‌سازی کامل Push Notification
- بهبود نمایش لوگو در تم لایت

**موفق باشید! 🚀**

