# راهنمای کامل ادغام Admin Panel با پروژه اصلی

## 📋 خلاصه
این راهنما نحوه اضافه کردن Admin Panel به پروژه اصلی ASL Market را توضیح می‌دهد.

## ✅ کارهای انجام شدهه

### 1. ایجاد API Service
- ✅ فایل `admin-panel/src/lib/api/adminApi.ts` ایجاد شد
- ✅ تمام endpoints برای اتصال به backend آماده است
- ✅ Authentication به backend واقعی متصل شد

### 2. به‌روزرسانی Authentication
- ✅ فایل `admin-panel/src/lib/utils/auth.ts` به‌روزرسانی شد
- ✅ از Mock API به Real API تغییر کرد

## 🔧 مراحل نصب و راه‌اندازی

### مرحله 1: نصب Dependencies
```bash
cd admin-panel
npm install
```

### مرحله 2: بررسی Dependencies مشترک
برخی از dependencies در هر دو پروژه وجود دارند. اگر conflict داشتید:
```bash
# در root پروژه
npm install
```

### مرحله 3: راه‌اندازی Admin Panel
```bash
cd admin-panel
npm run dev
```

Admin Panel روی پورت 8080 اجرا می‌شود (طبق vite.config.ts)

### مرحله 4: اتصال به Backend
Admin Panel به صورت خودکار به backend متصل می‌شود:
- Development: `http://localhost:8080/api/v1`
- Production: `https://asllmarket.com/backend/api/v1`

## 📁 ساختار فایل‌ها

```
asl_market/
├── admin-panel/              # Admin Panel (پروژه جداگانه)
│   ├── src/
│   │   ├── lib/
│   │   │   ├── api/
│   │   │   │   └── adminApi.ts    # ✅ API Service ایجاد شد
│   │   │   └── utils/
│   │   │       └── auth.ts         # ✅ به‌روزرسانی شد
│   │   ├── pages/                  # صفحات Admin Panel
│   │   └── components/             # کامپوننت‌های Admin Panel
│   └── package.json
├── src/                        # پروژه اصلی
│   ├── App.tsx
│   └── ...
└── backend/                    # Backend Go
```

## 🚀 نحوه استفاده

### 1. ورود به Admin Panel
1. Admin Panel را اجرا کنید: `cd admin-panel && npm run dev`
2. به آدرس `http://localhost:8080` بروید
3. با ایمیل و رمز عبور کاربر admin وارد شوید

### 2. قابلیت‌های موجود
- ✅ مدیریت کاربران
- ✅ مدیریت تأمین‌کنندگان
- ✅ مدیریت ویزیتورها
- ✅ مدیریت لایسنس‌ها
- ✅ مدیریت برداشت‌ها
- ✅ مدیریت تیکت‌های پشتیبانی
- ✅ مدیریت آموزش (ویدیوها)
- ✅ مدیریت محصولات
- ✅ مدیریت پاپ‌اپ‌ها
- ✅ مدیریت نوتیفیکیشن‌ها
- ✅ آمار و گزارش‌گیری
- ✅ خروجی Excel
- ✅ واردات گروهی

## ⚠️ نکات مهم

### 1. Authentication
- Admin Panel از همان token system پروژه اصلی استفاده می‌کند
- Token در `localStorage` با کلید `auth_token` ذخیره می‌شود
- برای ورود باید کاربر admin باشد (is_admin = true در دیتابیس)

### 2. Backend Endpoints
بسیاری از endpoints admin در backend وجود دارند:
- `/api/v1/admin/users`
- `/api/v1/admin/suppliers`
- `/api/v1/admin/visitors`
- `/api/v1/admin/licenses`
- `/api/v1/admin/withdrawals`
- `/api/v1/admin/support/tickets`
- و غیره...

### 3. Endpoints که باید اضافه شوند
برخی endpoints ممکن است در backend وجود نداشته باشند:
- `/api/v1/admin/users` - لیست کاربران
- `/api/v1/admin/users/:id` - جزئیات کاربر
- `/api/v1/admin/users/stats` - آمار کاربران
- `/api/v1/admin/dashboard/stats` - آمار داشبورد
- `/api/v1/admin/export/*` - خروجی Excel
- `/api/v1/admin/import/*` - واردات Excel

## 🔨 مراحل بعدی (اختیاری)

### گزینه 1: ادغام کامل در پروژه اصلی
اگر می‌خواهید Admin Panel را در همان پروژه اصلی داشته باشید:

1. فایل‌های admin-panel را به src اضافه کنید:
```bash
# کپی کردن فایل‌ها
cp -r admin-panel/src/pages src/admin-pages
cp -r admin-panel/src/components src/admin-components
```

2. Routing را در App.tsx اضافه کنید:
```tsx
// در src/App.tsx
import AdminDashboard from './admin-pages/Dashboard';
import AdminUsers from './admin-pages/Users';
// ... سایر صفحات

// در Routes
<Route path="/admin/*" element={<AdminRoutes />} />
```

### گزینه 2: نگه داشتن به عنوان پروژه جداگانه (پیشنهادی)
- Admin Panel به صورت جداگانه اجرا می‌شود
- روی پورت 8080
- مستقل از پروژه اصلی
- راحت‌تر برای توسعه و نگهداری

## 🐛 رفع مشکلات

### مشکل: خطای CORS
اگر خطای CORS گرفتید، مطمئن شوید که:
- Backend CORS را برای localhost:8080 فعال کرده است
- Proxy در vite.config.ts درست تنظیم شده است

### مشکل: خطای Authentication
- مطمئن شوید که کاربر admin است (is_admin = true)
- Token در localStorage ذخیره می‌شود
- Backend باید token را validate کند

### مشکل: Endpoint پیدا نشد
- برخی endpoints ممکن است در backend وجود نداشته باشند
- باید آنها را در backend اضافه کنید
- یا در adminApi.ts از endpoints موجود استفاده کنید

## 📝 چک‌لیست

- [x] API Service ایجاد شد
- [x] Authentication به backend متصل شد
- [ ] Backend endpoints بررسی شدند
- [ ] Endpoints مفقود اضافه شدند
- [ ] تست کامل انجام شد
- [ ] Documentation کامل شد

## 🎯 نتیجه

Admin Panel آماده استفاده است! فقط باید:
1. Dependencies را نصب کنید
2. Admin Panel را اجرا کنید
3. با کاربر admin وارد شوید
4. از تمام قابلیت‌ها استفاده کنید

اگر endpoint خاصی کار نمی‌کند، باید آن را در backend اضافه کنید.

