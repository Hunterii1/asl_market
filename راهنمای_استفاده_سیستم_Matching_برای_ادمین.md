# 📘 راهنمای استفاده از سیستم Matching برای ادمین

## 🎯 مقدمه

سیستم Matching یک سیستم هوشمند برای اتصال تأمین‌کنندگان به ویزیتورها است. این راهنما به شما کمک می‌کند تا به عنوان ادمین، از تمام قابلیت‌های این سیستم استفاده کنید.

---

## 📋 فهرست مطالب

1. [نحوه دسترسی به سیستم](#نحوه-دسترسی-به-سیستم)
2. [مدیریت درخواست‌های Matching](#مدیریت-درخواستهای-matching)
3. [مدیریت نوتیفیکیشن‌ها](#مدیریت-نوتیفیکیشنها)
4. [نظارت بر چت‌ها](#نظارت-بر-چتها)
5. [مدیریت امتیازها](#مدیریت-امتیازها)
6. [تنظیمات Push Notifications](#تنظیمات-push-notifications)

---

## 🔐 نحوه دسترسی به سیستم

### ورود به پنل ادمین

1. وارد پنل ادمین تلگرام شوید
2. از منوی اصلی، گزینه "Matching System" را انتخاب کنید
3. یا مستقیماً از طریق API به سیستم دسترسی داشته باشید

---

## 📊 مدیریت درخواست‌های Matching

### مشاهده تمام درخواست‌ها

به عنوان ادمین، می‌توانید تمام درخواست‌های Matching را مشاهده کنید:

**از طریق API:**
- `GET /api/v1/admin/matching/requests` - لیست تمام درخواست‌ها
- می‌توانید فیلتر کنید بر اساس:
  - وضعیت (pending, active, accepted, expired, cancelled)
  - تاریخ ایجاد
  - تأمین‌کننده

### وضعیت‌های درخواست

- **Pending**: درخواست تازه ایجاد شده و در انتظار matching
- **Active**: درخواست فعال و در حال ارسال به ویزیتورها
- **Accepted**: درخواست توسط یک ویزیتور پذیرفته شده
- **Expired**: درخواست منقضی شده (هیچ ویزیتوری نپذیرفته)
- **Cancelled**: درخواست توسط تأمین‌کننده لغو شده
- **Completed**: درخواست تکمیل شده

### مدیریت درخواست‌های مشکل‌دار

اگر درخواستی مشکل دارد:

1. **لغو درخواست**: می‌توانید درخواست را لغو کنید
2. **تمدید زمان**: می‌توانید زمان انقضا را تمدید کنید
3. **تغییر وضعیت**: می‌توانید وضعیت درخواست را تغییر دهید

---

## 🔔 مدیریت نوتیفیکیشن‌ها

### ایجاد نوتیفیکیشن عمومی

برای ارسال نوتیفیکیشن به همه کاربران:

1. وارد پنل ادمین تلگرام شوید
2. گزینه "Create Notification" را انتخاب کنید
3. اطلاعات زیر را وارد کنید:
   - **عنوان**: عنوان نوتیفیکیشن
   - **پیام**: متن پیام
   - **نوع**: نوع نوتیفیکیشن (matching, general, etc.)
   - **اولویت**: (normal, high, urgent)
   - **لینک عملیات**: (اختیاری) لینک صفحه‌ای که با کلیک باز می‌شود

4. نوتیفیکیشن به صورت خودکار ارسال می‌شود:
   - **Push Notification**: به تمام کاربرانی که Push فعال دارند
   - **In-App Notification**: در پنل کاربران نمایش داده می‌شود

### ایجاد نوتیفیکیشن برای کاربر خاص

برای ارسال نوتیفیکیشن به یک کاربر خاص:

1. در پنل ادمین، کاربر مورد نظر را پیدا کنید
2. گزینه "Send Notification" را انتخاب کنید
3. پیام را وارد کنید
4. نوتیفیکیشن فقط به آن کاربر ارسال می‌شود

### نظارت بر نوتیفیکیشن‌های Matching

سیستم Matching به صورت خودکار نوتیفیکیشن ارسال می‌کند:

- **هنگام ایجاد درخواست جدید**: به ویزیتورهای مناسب
- **هنگام پذیرش درخواست**: به تأمین‌کننده
- **هنگام ارسال پیام در چت**: به طرف مقابل

می‌توانید این نوتیفیکیشن‌ها را در جدول `matching_notifications` مشاهده کنید.

---

## 💬 نظارت بر چت‌ها

### مشاهده مکالمات

به عنوان ادمین، می‌توانید تمام مکالمات بین تأمین‌کنندگان و ویزیتورها را مشاهده کنید:

**از طریق API:**
- `GET /api/v1/admin/matching/chats` - لیست تمام چت‌ها
- `GET /api/v1/admin/matching/chats/:id/messages` - پیام‌های یک چت خاص

### نکات مهم

- چت‌ها فقط برای درخواست‌های **Accepted** فعال می‌شوند
- هر درخواست فقط یک چت دارد (بین تأمین‌کننده و ویزیتور پذیرنده)
- پیام‌ها به صورت Real-time به‌روزرسانی می‌شوند

### مدیریت چت‌های مشکل‌دار

اگر چتی مشکل دارد:

1. می‌توانید چت را غیرفعال کنید
2. می‌توانید پیام‌های خاص را حذف کنید (در صورت نیاز)
3. می‌توانید به کاربران کمک کنید

---

## ⭐ مدیریت امتیازها

### مشاهده امتیازها

می‌توانید تمام امتیازهای داده شده را مشاهده کنید:

**از طریق API:**
- `GET /api/v1/admin/matching/ratings` - لیست تمام امتیازها
- می‌توانید فیلتر کنید بر اساس:
  - کاربر
  - درخواست
  - امتیاز (1-5)

### آمار امتیازها

- **میانگین امتیاز هر کاربر**: برای مشاهده اعتبار کاربران
- **تعداد امتیازها**: تعداد کل امتیازهای داده شده
- **توزیع امتیازها**: چند درصد 5 ستاره، 4 ستاره و...

### مدیریت امتیازهای نامناسب

اگر امتیازی نامناسب است:

1. می‌توانید امتیاز را بررسی کنید
2. در صورت نیاز، امتیاز را حذف کنید
3. به کاربر اطلاع دهید

---

## 🔧 تنظیمات Push Notifications

### تنظیم VAPID Keys

برای فعال‌سازی Push Notifications:

1. وارد فایل `backend/config/config.yaml` شوید
2. بخش `push` را پیدا کنید:
   ```yaml
   push:
     vapid_public_key: "کلید عمومی شما"
     vapid_private_key: "کلید خصوصی شما"
     vapid_subject: "mailto:your-email@example.com"
   ```

3. کلیدهای VAPID را وارد کنید (از Firebase یا خودتان ایجاد کنید)

### تست Push Notifications

برای تست Push Notifications:

1. از طریق API: `POST /api/v1/push/test`
2. یا از پنل ادمین، گزینه "Test Push" را انتخاب کنید
3. یک نوتیفیکیشن تست به خودتان ارسال می‌شود

### نظارت بر Push Subscriptions

می‌توانید تعداد کاربرانی که Push فعال دارند را مشاهده کنید:

- **از طریق دیتابیس**: جدول `push_subscriptions`
- **آمار**: تعداد کل subscriptions فعال

---

## 📈 آمار و گزارش‌گیری

### آمار کلی Matching

می‌توانید آمار زیر را مشاهده کنید:

- **تعداد کل درخواست‌ها**: تعداد درخواست‌های ایجاد شده
- **نرخ پذیرش**: چند درصد درخواست‌ها پذیرفته شده‌اند
- **میانگین زمان پذیرش**: چقدر طول می‌کشد تا درخواستی پذیرفته شود
- **تعداد چت‌های فعال**: چند چت در حال حاضر فعال است
- **میانگین امتیاز**: میانگین امتیازهای داده شده

### گزارش‌های مفید

1. **گزارش درخواست‌های منقضی شده**: درخواست‌هایی که هیچ ویزیتوری نپذیرفته
2. **گزارش چت‌های بدون پاسخ**: چت‌هایی که پیامی ارسال نشده
3. **گزارش امتیازهای پایین**: کاربرانی که امتیاز پایین دریافت کرده‌اند

---

## 🛠️ عیب‌یابی

### مشکلات رایج

#### 1. نوتیفیکیشن ارسال نمی‌شود

**راه‌حل:**
- بررسی کنید که VAPID keys درست تنظیم شده باشند
- بررسی کنید که Service Worker در Frontend فعال باشد
- بررسی کنید که کاربر Push را فعال کرده باشد

#### 2. درخواست Matching نمی‌شود

**راه‌حل:**
- بررسی کنید که ویزیتورهای مناسب وجود داشته باشند
- بررسی کنید که الگوریتم Matching درست کار می‌کند
- بررسی لاگ‌های سیستم

#### 3. چت کار نمی‌کند

**راه‌حل:**
- بررسی کنید که درخواست Accepted شده باشد
- بررسی کنید که هر دو طرف (تأمین‌کننده و ویزیتور) دسترسی دارند
- بررسی دیتابیس برای چت

---

## 📞 پشتیبانی

اگر مشکلی دارید:

1. ابتدا این راهنما را کامل بخوانید
2. لاگ‌های سیستم را بررسی کنید
3. با تیم فنی تماس بگیرید

---

## ✅ چک‌لیست تست سیستم Matching

برای اطمینان از کارکرد صحیح سیستم:

- [ ] درخواست Matching ایجاد می‌شود
- [ ] ویزیتورهای مناسب پیدا می‌شوند
- [ ] نوتیفیکیشن‌ها ارسال می‌شوند (SMS, Push, In-App)
- [ ] ویزیتور می‌تواند درخواست را بپذیرد/رد کند/سوال بپرسد
- [ ] چت بین تأمین‌کننده و ویزیتور کار می‌کند
- [ ] امتیازدهی کار می‌کند
- [ ] Push Notifications کار می‌کند
- [ ] درخواست‌ها منقضی می‌شوند (در صورت عدم پذیرش)
- [ ] تمدید درخواست کار می‌کند
- [ ] لغو درخواست کار می‌کند

---

**آخرین به‌روزرسانی**: 2025

