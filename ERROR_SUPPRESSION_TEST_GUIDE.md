# 🧪 راهنمای تست سرکوب خطاها

## ✅ فیلتر دقیق فعال شد!

فقط خطاهای **خاص** سرکوب می‌شوند، بقیه خطاها **قرمز** نمایش داده می‌شوند.

---

## 🔴 رنگ Toast ها

### همه خطاها قرمز هستند!
```typescript
variant: "destructive"  // ← این یعنی قرمز ✅
```

---

## 🧪 تست‌های مختلف

### ✅ **خطاهای سرکوب شده** (Toast نمی‌آید)

#### 1️⃣ خطاهای ثبت‌نام (فارسی):
```
✅ "شما هنوز به عنوان ویزیتور ثبت‌نام نکرده‌اید"
✅ "شما هنوز به عنوان تأمین‌کننده ثبت‌نام نکرده‌اید"
✅ "هنوز به عنوان ویزیتور ثبت‌نام نکرده‌اید"
```

#### 2️⃣ خطاهای ثبت‌نام (انگلیسی + 404):
```
✅ "visitor not found"
✅ "supplier not found"
✅ "no visitor registration found"
```

#### 3️⃣ خطاهای Token (فقط اگر لاگین نیستید):
```
✅ "authorization token is required"
✅ "authorization header is required"
✅ "missing authorization header"
```

---

### ❌ **خطاهای نمایش داده می‌شوند** (Toast قرمز ✅)

#### 1️⃣ خطاهای شبکه:
```
🔴 "Failed to fetch"
🔴 "Network error"
🔴 "Connection failed"
```

#### 2️⃣ خطاهای سرور:
```
🔴 "Internal server error" (500)
🔴 "Service unavailable" (503)
🔴 "Bad gateway" (502)
```

#### 3️⃣ خطاهای Validation:
```
🔴 "Invalid email format"
🔴 "Password too short"
🔴 "Required field missing"
```

#### 4️⃣ خطاهای Database:
```
🔴 "Duplicate entry"
🔴 "Record not found" (اگر مربوط به visitor/supplier نباشد)
🔴 "Database error"
```

#### 5️⃣ خطاهای احراز هویت (اگر لاگین هستید):
```
🔴 "Invalid token"
🔴 "Token expired"
🔴 "Access denied"
```

#### 6️⃣ خطاهای لایسنس:
```
🔴 "License required"
🔴 "Invalid license"
🔴 "License expired"
```

#### 7️⃣ خطاهای 404 عمومی:
```
🔴 "Product not found"
🔴 "Order not found"
🔴 "User not found"
```

---

## 🧪 سناریوهای تست

### تست 1: کاربر ثبت‌نام نکرده
```bash
# ورود به /visitor-status بدون ثبت‌نام

نتیجه:
  ❌ Toast قرمز نمی‌آید
  ✅ Alert آبی در صفحه نمایش داده می‌شود
  ✅ Console: "🔇 Suppressing FA registration error"
```

### تست 2: خطای شبکه
```bash
# قطع کردن اینترنت و تلاش برای بارگذاری

نتیجه:
  ✅ Toast قرمز نمایش داده می‌شود
  🔴 "خطای شبکه: اتصال به سرور برقرار نشد"
```

### تست 3: Token خطا (بدون login)
```bash
# رفتن به صفحه محافظت شده بدون login

نتیجه:
  ❌ Toast قرمز نمی‌آید (سرکوب می‌شود)
  ✅ Redirect به /login
  ✅ Console: "🔇 Suppressing token error"
```

### تست 4: Token خطا (با login اما expired)
```bash
# Token منقضی شده داریم

نتیجه:
  ✅ Toast قرمز نمایش داده می‌شود
  🔴 "Token expired"
  ✅ Redirect به /login
```

### تست 5: خطای Validation
```bash
# فرم با فیلد خالی submit کنیم

نتیجه:
  ✅ Toast قرمز نمایش داده می‌شود
  🔴 "Required field missing"
```

### تست 6: خطای 500
```bash
# Backend خطای داخلی دارد

نتیجه:
  ✅ Toast قرمز نمایش داده می‌شود
  🔴 "خطای سرور"
```

### تست 7: خطای 404 دیگر
```bash
# محصول یافت نشد

نتیجه:
  ✅ Toast قرمز نمایش داده می‌شود
  🔴 "Product not found"
```

---

## 🎯 تفاوت دقیق

### ❌ فیلتر قبلی (خیلی گسترده):
```typescript
// هر 404 که "visitor" داشت سرکوب می‌شد!
if (statusCode === 404 && messageLower.includes('visitor')) {
  return true; // ❌ خیلی گسترده
}
```

**مشکل**: 
- "Visitor order not found" → سرکوب می‌شد ❌
- "Visitor product not found" → سرکوب می‌شد ❌

### ✅ فیلتر جدید (دقیق):
```typescript
// فقط خطاهای دقیق ثبت‌نام سرکوب می‌شود
if (messageLower === 'visitor not found') {
  return true; // ✅ دقیق
}
```

**نتیجه**:
- "visitor not found" → سرکوب می‌شود ✅
- "Visitor order not found" → نمایش داده می‌شود ✅
- "Visitor product not found" → نمایش داده می‌شود ✅

---

## 🔍 بررسی در Console

### خطا سرکوب شد:
```
Console:
🔇 Suppressing FA registration error: شما هنوز ثبت‌نام نکرده‌اید
⏭️ Suppressing registration reminder error: شما هنوز ثبت‌نام نکرده‌اید
```

### خطا نمایش داده شد:
```
Console:
❌ API Error: { response: { status: 500, data: ... } }
```

```
Toast:
🔴 خطای سرور
Internal server error
```

---

## 📊 جدول تست

| خطا | کد | سرکوب؟ | Toast قرمز؟ |
|-----|-----|---------|-------------|
| "ثبت‌نام نکرده" | 404 | ✅ بله | ❌ خیر |
| "visitor not found" | 404 | ✅ بله | ❌ خیر |
| "token required" (no login) | 401 | ✅ بله | ❌ خیر |
| "token expired" (logged in) | 401 | ❌ خیر | ✅ بله 🔴 |
| "Network error" | - | ❌ خیر | ✅ بله 🔴 |
| "Invalid email" | 400 | ❌ خیر | ✅ بله 🔴 |
| "Server error" | 500 | ❌ خیر | ✅ بله 🔴 |
| "Product not found" | 404 | ❌ خیر | ✅ بله 🔴 |
| "License expired" | 403 | ❌ خیر | ✅ بله 🔴 |

---

## 💡 نکات مهم

### 1. رنگ همیشه قرمز است
```typescript
variant: "destructive"  // ✅ قرمز برای همه خطاها
```

### 2. فقط خطاهای خاص سرکوب می‌شوند
```typescript
// ✅ دقیق
if (messageLower === 'visitor not found') { ... }

// ❌ نه اینطور
if (messageLower.includes('visitor')) { ... }
```

### 3. Token فقط برای غیرلاگین سرکوب می‌شود
```typescript
const isLoggedIn = localStorage.getItem('token') !== null;
if (!isLoggedIn) {
  // فقط اینجا سرکوب می‌شود
}
```

### 4. Logging همیشه فعال است
```typescript
// حتی اگر سرکوب شود، در console لاگ می‌شود
console.log('🔇 Suppressing error:', errorMessage);
```

---

## ✅ چک‌لیست تست

- [ ] خطای "ثبت‌نام نکرده" سرکوب می‌شود
- [ ] خطای "visitor not found" سرکوب می‌شود  
- [ ] خطای "token required" (بدون login) سرکوب می‌شود
- [ ] خطای "Network error" قرمز نمایش داده می‌شود
- [ ] خطای "Server error" قرمز نمایش داده می‌شود
- [ ] خطای "Invalid email" قرمز نمایش داده می‌شود
- [ ] خطای "Product not found" قرمز نمایش داده می‌شود
- [ ] خطای "Token expired" (با login) قرمز نمایش داده می‌شود

---

## 🎉 نتیجه

✅ فیلتر دقیق کار می‌کند  
✅ فقط خطاهای خاص سرکوب می‌شوند  
✅ بقیه خطاها قرمز نمایش داده می‌شوند  
✅ رنگ همیشه قرمز است  

**همه چیز درست کار می‌کند! 🚀**
