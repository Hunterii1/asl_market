# Admin Panel - ASL Market

پنل مدیریت کامل برای ASL Market که تمام قابلیت‌های پنل تلگرام را در وب ارائه می‌دهد.

## 🚀 راه‌اندازی سریع

### 1. نصب Dependencies
```bash
npm install
```

### 2. اجرای Development Server
```bash
npm run dev
```

Admin Panel روی `http://localhost:8080` اجرا می‌شود.

### 3. ورود به سیستم
- ایمیل: ایمیل کاربر admin شما
- رمز عبور: رمز عبور کاربر admin

**نکته:** کاربر باید `is_admin = true` در دیتابیس داشته باشد.

## 📁 ساختار پروژه

```
admin-panel/
├── src/
│   ├── lib/
│   │   ├── api/
│   │   │   └── adminApi.ts      # API Service برای اتصال به Backend
│   │   └── utils/
│   │       └── auth.ts           # Authentication Utilities
│   ├── pages/                    # صفحات Admin Panel
│   │   ├── Dashboard.tsx        # داشبورد اصلی
│   │   ├── Users.tsx            # مدیریت کاربران
│   │   ├── Suppliers.tsx        # مدیریت تأمین‌کنندگان
│   │   ├── Visitors.tsx         # مدیریت ویزیتورها
│   │   ├── Licenses.tsx         # مدیریت لایسنس‌ها
│   │   ├── Withdrawals.tsx     # مدیریت برداشت‌ها
│   │   ├── Tickets.tsx          # مدیریت تیکت‌ها
│   │   ├── Education.tsx        # مدیریت آموزش
│   │   ├── Products.tsx         # مدیریت محصولات
│   │   ├── Popups.tsx           # مدیریت پاپ‌اپ‌ها
│   │   ├── Notifications.tsx    # مدیریت نوتیفیکیشن‌ها
│   │   ├── Admins.tsx           # مدیریت ادمین‌ها
│   │   ├── Statistics.tsx       # آمار و گزارش‌ها
│   │   ├── Export.tsx           # خروجی Excel
│   │   └── Settings.tsx          # تنظیمات
│   └── components/               # کامپوننت‌های قابل استفاده مجدد
│       ├── layout/              # Layout Components
│       ├── users/               # User Management Components
│       ├── suppliers/           # Supplier Management Components
│       └── ...
└── package.json
```

## 🔌 اتصال به Backend

Admin Panel به صورت خودکار به Backend متصل می‌شود:

- **Development:** `http://localhost:8080/api/v1` (از طریق proxy)
- **Production:** `https://asllmarket.com/backend/api/v1`

### API Base URL
API Base URL به صورت خودکار بر اساس hostname تعیین می‌شود:
- `localhost` یا `127.0.0.1` → `/api/v1` (proxy)
- `asllmarket.com` → `https://asllmarket.com/backend/api/v1`

## 🔑 Authentication

### نحوه کار
1. کاربر با ایمیل و رمز عبور وارد می‌شود
2. Backend token را برمی‌گرداند
3. Token در `localStorage` با کلید `auth_token` ذخیره می‌شود
4. در هر درخواست، token در header `Authorization: Bearer <token>` ارسال می‌شود

### Session Management
- Session در `localStorage` با کلید `asll-session` ذخیره می‌شود
- شامل: user info, token, expiresAt, rememberMe
- Session بعد از expire خودکار پاک می‌شود

## 📋 قابلیت‌ها

### ✅ مدیریت کاربران
- مشاهده لیست کاربران (با فیلتر و pagination)
- جستجوی کاربر
- اضافه/ویرایش/حذف کاربر
- آمار کاربران
- خروجی Excel

### ✅ مدیریت تأمین‌کنندگان
- مشاهده لیست (pending/approved/rejected/all/featured)
- تأیید/رد تأمین‌کننده
- ویرایش/حذف
- برگزیده کردن
- جستجو
- آمار

### ✅ مدیریت ویزیتورها
- مشابه تأمین‌کنندگان
- تأیید/رد/ویرایش/حذف
- برگزیده کردن

### ✅ مدیریت لایسنس‌ها
- تولید لایسنس (Plus, Pro, Plus4)
- مشاهده لیست لایسنس‌ها
- حذف لایسنس
- آمار لایسنس‌ها

### ✅ مدیریت برداشت‌ها
- مشاهده لیست (pending/approved/processing/completed/rejected)
- تأیید/رد/پردازش/تکمیل
- وارد کردن شماره حساب مقصد
- آمار

### ✅ مدیریت تیکت‌های پشتیبانی
- مشاهده لیست (open/in_progress/waiting_response/closed/all)
- پاسخ به تیکت
- بستن تیکت
- آمار

### ✅ مدیریت آموزش
- مدیریت دسته‌بندی‌ها
- اضافه/ویرایش/حذف ویدیو
- آپلود فایل یا لینک
- آمار بازدید

### ✅ مدیریت محصولات
- مشاهده لیست محصولات
- اضافه/ویرایش/حذف
- تغییر وضعیت

### ✅ مدیریت پاپ‌اپ‌ها
- اضافه/ویرایش/حذف
- فعال/غیرفعال

### ✅ مدیریت نوتیفیکیشن‌ها
- ارسال نوتیفیکیشن
- تاریخچه
- آمار

### ✅ آمار و گزارش‌گیری
- آمار کلی سیستم
- نمودارها و چارت‌ها
- گزارش‌های مختلف

### ✅ خروجی Excel
- خروجی کاربران
- خروجی تأمین‌کنندگان
- خروجی ویزیتورها
- خروجی محصولات

### ✅ واردات گروهی
- وارد کردن تأمین‌کنندگان از Excel
- وارد کردن ویزیتورها از Excel
- وارد کردن محصولات از Excel

## 🔧 تنظیمات

### Port
پورت پیش‌فرض: `8080`
می‌توانید در `vite.config.ts` تغییر دهید.

### API Configuration
API Base URL در `src/lib/api/adminApi.ts` تنظیم می‌شود.

## 🐛 رفع مشکلات

### خطای CORS
اگر خطای CORS گرفتید:
1. مطمئن شوید backend CORS را برای `localhost:8080` فعال کرده است
2. Proxy در `vite.config.ts` درست تنظیم شده باشد

### خطای Authentication
1. مطمئن شوید کاربر `is_admin = true` دارد
2. Token در localStorage ذخیره می‌شود
3. Backend باید token را validate کند

### Endpoint پیدا نشد
برخی endpoints ممکن است در backend وجود نداشته باشند. باید آنها را اضافه کنید یا از endpoints موجود استفاده کنید.

## 📝 TODO

- [ ] اضافه کردن endpoint برای مدیریت کاربران در backend
- [ ] اضافه کردن endpoint برای آمار داشبورد
- [ ] اضافه کردن endpoints برای export/import
- [ ] تست کامل تمام قابلیت‌ها
- [ ] اضافه کردن role-based access control

## 🎯 نکات مهم

1. **امنیت:** تمام routes با `ProtectedRoute` محافظت می‌شوند
2. **Token Management:** Token به صورت خودکار در هر request اضافه می‌شود
3. **Error Handling:** خطاها به صورت مناسب handle می‌شوند
4. **Loading States:** برای تمام عملیات async loading state وجود دارد
5. **Responsive:** UI برای موبایل و تبلت responsive است

## 📚 مستندات بیشتر

برای اطلاعات بیشتر به فایل `ADMIN_PANEL_INTEGRATION_GUIDE.md` در root پروژه مراجعه کنید.
