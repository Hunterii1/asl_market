# 📋 نیازمندی‌های اضافی - ASL Market

## 1️⃣ بررسی و رفع مشکل ساخت کاربر

### مشکل
گاهی اوقات در فرآیند ثبت‌نام کاربران در اصل مارکت مشکلاتی رخ می‌دهد.

### اقدامات
- ✅ بررسی کدهای ثبت‌نام (`auth_controller.go`, `public_registration_controller.go`)
- ✅ بررسی duplicate check برای شماره تلفن و ایمیل
- ✅ بهبود transaction handling
- ✅ اضافه کردن logging
- ✅ تست سناریوهای مختلف

### API های مورد بررسی
```
POST /api/v1/auth/register
POST /api/v1/public/supplier/register
POST /api/v1/public/visitor/register
```

---

## 2️⃣ سیستم توزیع درخواست‌ها (مثل اجاره)

### نیازمندی
**توزیع عادلانه درخواست‌ها بین ویزیتورها**

### مثال
- 50 درخواست موجود
- 5 ویزیتور مناسب
- **نتیجه: هر ویزیتور 10 درخواست دریافت می‌کند** (50 ÷ 5 = 10)

### قوانین
- توزیع به صورت round-robin
- اگر تقسیم نشد، باقیمانده به صورت عادلانه توزیع شود
- هر ویزیتور باید تعداد مساوی درخواست دریافت کند

### پیاده‌سازی
- در API `POST /api/v1/matching/auto-match/:request_id`
- الگوریتم توزیع در Backend
- ثبت توزیع در دیتابیس

---

## 3️⃣ Push Notification در وب اپ

### وضعیت فعلی
- ✅ PWA با `vite-plugin-pwa` پیاده‌سازی شده
- ✅ Service Worker (`sw.js`) وجود دارد
- ✅ Manifest.json تنظیم شده

### نیازمندی‌ها
- بررسی پشتیبانی مرورگر از Web Push API
- یکپارچه‌سازی با FCM یا Web Push API
- ثبت subscription کاربران
- ارسال Push Notification از Backend

### API های مورد نیاز
```
POST /api/v1/notifications/subscribe
POST /api/v1/notifications/unsubscribe
GET  /api/v1/notifications/subscription-status
```

### Frontend
- درخواست مجوز Notification
- ثبت Service Worker
- مدیریت Push events
- نمایش Notification

---

## 4️⃣ لوگو تم لایت

### مشکل
لوگو باید در تم لایت (light theme) به درستی نمایش داده شود.

### راه‌حل‌ها

**گزینه 1: دو نسخه لوگو**
- `/LOGO-light.png` برای تم لایت
- `/LOGO-dark.png` برای تم دارک

**گزینه 2: CSS Filter**
```css
.logo-light { filter: brightness(0) invert(1); }
.logo-dark { filter: brightness(0); }
```

**گزینه 3: SVG**
- استفاده از SVG که رنگ آن بر اساس تم تغییر می‌کند

### صفحات مورد بررسی
- Header (تمام صفحات)
- صفحه اصلی
- صفحات ثبت‌نام
- صفحات لاگین

---

## ✅ چک‌لیست پیاده‌سازی

### 1. بررسی ساخت کاربر
- [ ] بررسی کدهای ثبت‌نام
- [ ] رفع مشکلات duplicate
- [ ] بهبود error handling
- [ ] تست کامل

### 2. توزیع درخواست‌ها
- [ ] پیاده‌سازی الگوریتم توزیع
- [ ] تست با تعداد مختلف درخواست و ویزیتور
- [ ] اطمینان از توزیع عادلانه

### 3. Push Notification
- [ ] بررسی پشتیبانی مرورگر
- [ ] پیاده‌سازی subscription
- [ ] یکپارچه‌سازی با Backend
- [ ] تست ارسال Notification

### 4. لوگو تم لایت
- [ ] بررسی نمایش لوگو در تم لایت
- [ ] پیاده‌سازی راه‌حل مناسب
- [ ] تست در تمام صفحات
- [ ] تست در مرورگرهای مختلف

---

**موفق باشید! 🚀**


